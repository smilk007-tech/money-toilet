# Vercel 환경변수 예시 — Vercel 대시보드 Settings > Environment Variables에 등록
# (이 파일은 예시일 뿐, 실제 값은 커밋 금지)

# ── 필수 ──────────────────────────────────────────────────────────────────────

# 소켓 서버 URL (Railway 배포 주소)
NEXT_PUBLIC_SOCKET_URL=https://your-socket-server.railway.app

# 실시간 소켓 활성화 ("1" = 활성, 없으면 비활성/로컬 페이크소켓)
NEXT_PUBLIC_RT=1

# 프론트 도메인 (공유 링크 생성에 사용)
NEXT_PUBLIC_SITE_URL=https://moneytoilet.kr

# Upstash Redis (어드민 과거 통계 직접 조회용, 소켓서버와 같은 인스턴스)
KV_REST_API_URL=https://xxxx.upstash.io
KV_REST_API_TOKEN=xxxxxxxx

# ── 선택 ──────────────────────────────────────────────────────────────────────

# 시스템 공지 배너 (상단 마퀴 스크롤) — JSON 배열, 없으면 배너 미표시
# start/end 생략 가능. 날짜만 쓰면 KST 기준 당일 00:00~23:59.
# url 있으면 클릭 시 이동, 없으면 클릭 무반응.
#
# 예시:
# NEXT_PUBLIC_NOTICES=[{"text":"7/5 오전 10시~12시 서버 점검 예정","start":"2026-07-05","end":"2026-07-05T12:00"},{"text":"신규 이벤트 오픈 🎉","start":"2026-07-10","url":"https://moneytoilet.kr"}]
NEXT_PUBLIC_NOTICES=
